#pragma once

// Experimental v2 API for building schemas as objects instead of template
// specializations. This is intentionally minimal for now and will be expanded
// as the design settles.
namespace aison2 {

struct SchemaDescriptor {
  // Placeholder for future schema metadata; kept trivial so usage scaffolding
  // compiles while we iterate on the API surface.
};

}  // namespace aison2
